<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE struts PUBLIC
	"-//Apache Software Foundation//DTD Struts Configuration 2.3//EN"
	"http://struts.apache.org/dtds/struts-2.3.dtd">

<struts>

<!-- struts2 默认的配置文件 default.properties.位置在 struts2-core-2.x.x.jar 的 org.apache.struts2 包 下面的 -->

<!-- 设置浏览器是否缓存静态内容，默认值为true（生产环境下使用），开发阶段最好关闭 -->
<constant name="struts.serve.static.browserCache" value="false" />

<!-- 当struts的配置文件修改后，系统是否自动重新加载该文件，默认值为false（生产环境下使用），开发阶段最好打开 -->
<constant name="struts.configuration.xml.reload" value="true" />

<!-- 开发模式下使用，这样可以打印出更详细的错误信息 -->
<constant name="struts.devMode" value="true" />

<!-- 默认的视图主题 -->
<constant name="struts.ui.theme" value="simple" />

<!-- 该属性指定Struts 2中的action由Spring容器创建 -->
<constant name="struts.objectFactory" value="spring" />

<!-- 该属性指定Struts 2应用所需要的国际化资源文件，如果有多份国际化资源文件，则多个资源文件的文件名以英文逗号（,）隔开。 -->
<constant name="struts.custom.i18n.resources" value="message" />

	<package name="default" extends="struts-default" namespace="/">
		<default-action-ref name="error"></default-action-ref>
		<global-results>
			<result type="redirectAction" name="home">
                <param name="namespace">/</param>
                <param name="actionName">home</param>
			</result>
			<result name="exception">/error.jsp?type=3</result>
		</global-results>
	<!-- 
		<global-exception-mappings>
			<exception-mapping result="exception" exception="java.lang.Exception"></exception-mapping>
		</global-exception-mappings> -->
		<action name="home" class="info.yinhua.web.action.HomeAction"
			method="init">
			<result>/WEB-INF/jsp/index.jsp</result>
		</action>
		<action name="forward">
			<result>/WEB-INF/jsp/index.jsp</result>
		</action>
		<action name="error">
			<result>/WEB-INF/jsp/common/error.jsp?type=4</result>
		</action>
	</package>

	<package name="login" extends="default" namespace="/login">
		<action name="login" class="info.yinhua.web.action.LoginAction"
			method="login">
			<result name="success">/WEB-INF/jsp/login.jsp</result>
		</action>
	</package>
	
	<package name="bom" extends="default" namespace="/bom">
		<default-action-ref name="init"></default-action-ref>
		<global-results>
			<result name="error">/WEB-INF/jsp/bom.jsp</result>
			<result name="success">/WEB-INF/jsp/bom.jsp</result>
		</global-results>
		<action name="init" class="info.yinhua.web.action.bom.BomAction"
			method="init" />
		<action name="transform" class="info.yinhua.web.action.bom.BomAction"
			method="transform" />
		<action name="search" class="info.yinhua.web.action.bom.BomAction"
			method="search" />
		<action name="list" class="info.yinhua.web.action.bom.BomAction"
			method="list" />
	</package>
</struts>
